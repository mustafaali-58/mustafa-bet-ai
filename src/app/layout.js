import './globals.css';

export const metadata = {
  title: 'Mustafa Ali Solmazgül',
  description: 'Personal portfolio and blog',
};

export default function RootLayout({ children }) {
  return children;
}
